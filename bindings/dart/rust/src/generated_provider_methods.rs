// Generated by `cargo run -p maxt-bindings-codegen`. Do not edit.

pub use crate::stream::{
    NativeBinanceAccountSubscription, NativeBinanceMarketSubscription,
    NativeBithumbAccountSubscription, NativeBithumbMarketSubscription,
    NativeUpbitAccountSubscription, NativeUpbitMarketSubscription, WireBinanceAccountStreamItem,
    WireBinanceMarketStreamItem, WireBithumbAccountStreamItem, WireBithumbMarketStreamItem,
    WireUpbitAccountStreamItem, WireUpbitMarketStreamItem,
};

impl NativeClient {
    /// Upbit 전용 API인 `subscribeDetailed`를 호출합니다.
    pub async fn upbit_subscribe_detailed(
        &self,
        subscription: WireSubscription,
    ) -> Result<NativeUpbitMarketSubscription, NativeError> {
        let subscription = subscription.into();
        let adapter = match self.built_in("upbit_subscribe_detailed")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .subscribe_detailed(&subscription)
            .await
            .map(NativeUpbitMarketSubscription::new)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `subscribeDetailedWith`를 호출합니다.
    pub async fn upbit_subscribe_detailed_with(
        &self,
        subscription: WireSubscription,
        config: WireStreamConfig,
    ) -> Result<NativeUpbitMarketSubscription, NativeError> {
        let subscription = subscription.into();
        let config = config.into();
        let adapter = match self.built_in("upbit_subscribe_detailed_with")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .subscribe_detailed_with(&subscription, &config)
            .await
            .map(NativeUpbitMarketSubscription::new)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `subscribeDetailedAccount`를 호출합니다.
    pub async fn upbit_subscribe_detailed_account(
        &self,
    ) -> Result<NativeUpbitAccountSubscription, NativeError> {
        let adapter = match self.built_in("upbit_subscribe_detailed_account")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .subscribe_detailed_account()
            .await
            .map(NativeUpbitAccountSubscription::new)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `subscribeDetailedAccountWith`를 호출합니다.
    pub async fn upbit_subscribe_detailed_account_with(
        &self,
        config: WireStreamConfig,
    ) -> Result<NativeUpbitAccountSubscription, NativeError> {
        let config = config.into();
        let adapter = match self.built_in("upbit_subscribe_detailed_account_with")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .subscribe_detailed_account_with(&config)
            .await
            .map(NativeUpbitAccountSubscription::new)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `testOrderDetail`를 호출합니다.
    pub async fn upbit_test_order_detail(
        &self,
        request: WireOrderRequest,
    ) -> Result<WireUpbitOrderResponse, NativeError> {
        let request: maxt::OrderRequest = request.try_into()?;
        let adapter = match self.built_in("upbit_test_order_detail")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .test_order_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `placeOrderDetail`를 호출합니다.
    pub async fn upbit_place_order_detail(
        &self,
        request: WireOrderRequest,
    ) -> Result<WireUpbitOrderResponse, NativeError> {
        let request: maxt::OrderRequest = request.try_into()?;
        let adapter = match self.built_in("upbit_place_order_detail")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .place_order_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `cancelOrderDetail`를 호출합니다.
    pub async fn upbit_cancel_order_detail(
        &self,
        market: WireMarket,
        order_id: String,
    ) -> Result<WireUpbitOrderResponse, NativeError> {
        let market = market.into();
        let adapter = match self.built_in("upbit_cancel_order_detail")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .cancel_order_detail(&market, &order_id)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `cancelOrderByClientIdDetail`를 호출합니다.
    pub async fn upbit_cancel_order_by_client_id_detail(
        &self,
        market: WireMarket,
        client_id: String,
    ) -> Result<WireUpbitOrderResponse, NativeError> {
        let market = market.into();
        let adapter = match self.built_in("upbit_cancel_order_by_client_id_detail")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .cancel_order_by_client_id_detail(&market, &client_id)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `ordersByIdsDetail`를 호출합니다.
    pub async fn upbit_orders_by_ids_detail(
        &self,
        request: WireOrderLookupRequest,
    ) -> Result<Vec<WireUpbitOrderResponse>, NativeError> {
        let request: maxt::OrderLookupRequest = request.try_into()?;
        let adapter = match self.built_in("upbit_orders_by_ids_detail")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .orders_by_ids_detail(&request)
            .await
            .map(|items| items.into_iter().map(Into::into).collect())
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `cancelOrdersDetail`를 호출합니다.
    pub async fn upbit_cancel_orders_detail(
        &self,
        request: WireCancelOrdersRequest,
    ) -> Result<WireUpbitCancelOrdersResponse, NativeError> {
        let request: maxt::CancelOrdersRequest = request.try_into()?;
        let adapter = match self.built_in("upbit_cancel_orders_detail")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .cancel_orders_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `depositDetail`를 호출합니다.
    pub async fn upbit_deposit_detail(
        &self,
        request: WireTransferLookupRequest,
    ) -> Result<WireUpbitDepositResponse, NativeError> {
        let request: maxt::TransferLookupRequest = request.try_into()?;
        let adapter = match self.built_in("upbit_deposit_detail")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .deposit_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `withdrawalDetail`를 호출합니다.
    pub async fn upbit_withdrawal_detail(
        &self,
        request: WireTransferLookupRequest,
    ) -> Result<WireUpbitWithdrawalResponse, NativeError> {
        let request: maxt::TransferLookupRequest = request.try_into()?;
        let adapter = match self.built_in("upbit_withdrawal_detail")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .withdrawal_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `cancelWithdrawalDetail`를 호출합니다.
    pub async fn upbit_cancel_withdrawal_detail(
        &self,
        withdrawal_id: String,
    ) -> Result<WireUpbitCancelWithdrawalResponse, NativeError> {
        let adapter = match self.built_in("upbit_cancel_withdrawal_detail")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .cancel_withdrawal_detail(&withdrawal_id)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Upbit 전용 API인 `cancelAndNewOrderDetail`를 호출합니다.
    pub async fn upbit_cancel_and_new_order_detail(
        &self,
        request: WireUpbitCancelAndNewOrderRequest,
    ) -> Result<WireUpbitCancelAndNewOrderDetailResult, NativeError> {
        let request: maxt::UpbitCancelAndNewOrderRequest = request.try_into()?;
        let adapter = match self.built_in("upbit_cancel_and_new_order_detail")? {
            BuiltInAdapter::Upbit(adapter) => adapter,
            _ => return Err(provider_mismatch("Upbit")),
        };
        adapter
            .cancel_and_new_order_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `orderBookSnapshot`를 호출합니다.
    pub async fn bithumb_order_book_snapshot(
        &self,
        market: WireMarket,
        depth: Option<u32>,
    ) -> Result<WireBithumbOrderBookSnapshot, NativeError> {
        let market = market.into();
        let adapter = match self.built_in("bithumb_order_book_snapshot")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .order_book_snapshot(&market, depth)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `subscribeDetailed`를 호출합니다.
    pub async fn bithumb_subscribe_detailed(
        &self,
        subscription: WireSubscription,
    ) -> Result<NativeBithumbMarketSubscription, NativeError> {
        let subscription = subscription.into();
        let adapter = match self.built_in("bithumb_subscribe_detailed")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .subscribe_detailed(&subscription)
            .await
            .map(NativeBithumbMarketSubscription::new)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `subscribeDetailedWith`를 호출합니다.
    pub async fn bithumb_subscribe_detailed_with(
        &self,
        subscription: WireSubscription,
        config: WireStreamConfig,
    ) -> Result<NativeBithumbMarketSubscription, NativeError> {
        let subscription = subscription.into();
        let config = config.into();
        let adapter = match self.built_in("bithumb_subscribe_detailed_with")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .subscribe_detailed_with(&subscription, &config)
            .await
            .map(NativeBithumbMarketSubscription::new)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `subscribeDetailedAccount`를 호출합니다.
    pub async fn bithumb_subscribe_detailed_account(
        &self,
    ) -> Result<NativeBithumbAccountSubscription, NativeError> {
        let adapter = match self.built_in("bithumb_subscribe_detailed_account")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .subscribe_detailed_account()
            .await
            .map(NativeBithumbAccountSubscription::new)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `subscribeDetailedAccountWith`를 호출합니다.
    pub async fn bithumb_subscribe_detailed_account_with(
        &self,
        config: WireStreamConfig,
    ) -> Result<NativeBithumbAccountSubscription, NativeError> {
        let config = config.into();
        let adapter = match self.built_in("bithumb_subscribe_detailed_account_with")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .subscribe_detailed_account_with(&config)
            .await
            .map(NativeBithumbAccountSubscription::new)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `ordersByIdsDetail`를 호출합니다.
    pub async fn bithumb_orders_by_ids_detail(
        &self,
        request: WireOrderLookupRequest,
    ) -> Result<WireBithumbOrdersResponse, NativeError> {
        let request: maxt::OrderLookupRequest = request.try_into()?;
        let adapter = match self.built_in("bithumb_orders_by_ids_detail")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .orders_by_ids_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `placeOrderDetail`를 호출합니다.
    pub async fn bithumb_place_order_detail(
        &self,
        request: WireOrderRequest,
    ) -> Result<WireBithumbOrderResponse, NativeError> {
        let request: maxt::OrderRequest = request.try_into()?;
        let adapter = match self.built_in("bithumb_place_order_detail")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .place_order_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `cancelOrderDetail`를 호출합니다.
    pub async fn bithumb_cancel_order_detail(
        &self,
        market: WireMarket,
        order_id: String,
    ) -> Result<WireBithumbCancelOrderResponse, NativeError> {
        let market = market.into();
        let adapter = match self.built_in("bithumb_cancel_order_detail")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .cancel_order_detail(&market, &order_id)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `cancelOrderByClientIdDetail`를 호출합니다.
    pub async fn bithumb_cancel_order_by_client_id_detail(
        &self,
        market: WireMarket,
        client_id: String,
    ) -> Result<WireBithumbCancelOrderResponse, NativeError> {
        let market = market.into();
        let adapter = match self.built_in("bithumb_cancel_order_by_client_id_detail")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .cancel_order_by_client_id_detail(&market, &client_id)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `cancelOrdersDetail`를 호출합니다.
    pub async fn bithumb_cancel_orders_detail(
        &self,
        request: WireCancelOrdersRequest,
    ) -> Result<WireBithumbCancelOrdersResponse, NativeError> {
        let request: maxt::CancelOrdersRequest = request.try_into()?;
        let adapter = match self.built_in("bithumb_cancel_orders_detail")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .cancel_orders_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `depositDetail`를 호출합니다.
    pub async fn bithumb_deposit_detail(
        &self,
        request: WireTransferLookupRequest,
    ) -> Result<WireBithumbDepositResponse, NativeError> {
        let request: maxt::TransferLookupRequest = request.try_into()?;
        let adapter = match self.built_in("bithumb_deposit_detail")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .deposit_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `withdrawalDetail`를 호출합니다.
    pub async fn bithumb_withdrawal_detail(
        &self,
        request: WireTransferLookupRequest,
    ) -> Result<WireBithumbWithdrawalResponse, NativeError> {
        let request: maxt::TransferLookupRequest = request.try_into()?;
        let adapter = match self.built_in("bithumb_withdrawal_detail")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .withdrawal_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Bithumb 전용 API인 `cancelWithdrawalDetail`를 호출합니다.
    pub async fn bithumb_cancel_withdrawal_detail(
        &self,
        withdrawal_id: String,
    ) -> Result<WireBithumbCancelWithdrawalResponse, NativeError> {
        let adapter = match self.built_in("bithumb_cancel_withdrawal_detail")? {
            BuiltInAdapter::Bithumb(adapter) => adapter,
            _ => return Err(provider_mismatch("Bithumb")),
        };
        adapter
            .cancel_withdrawal_detail(&withdrawal_id)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Binance 전용 API인 `placeOrderDetail`를 호출합니다.
    pub async fn binance_place_order_detail(
        &self,
        request: WireOrderRequest,
    ) -> Result<WireBinanceOrderResponse, NativeError> {
        let request: maxt::OrderRequest = request.try_into()?;
        let adapter = match self.built_in("binance_place_order_detail")? {
            BuiltInAdapter::Binance(adapter) => adapter,
            _ => return Err(provider_mismatch("Binance")),
        };
        adapter
            .place_order_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Binance 전용 API인 `cancelOrderDetail`를 호출합니다.
    pub async fn binance_cancel_order_detail(
        &self,
        market: WireMarket,
        order_id: String,
    ) -> Result<WireBinanceOrderResponse, NativeError> {
        let market = market.into();
        let adapter = match self.built_in("binance_cancel_order_detail")? {
            BuiltInAdapter::Binance(adapter) => adapter,
            _ => return Err(provider_mismatch("Binance")),
        };
        adapter
            .cancel_order_detail(&market, &order_id)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Binance 전용 API인 `cancelOrderByClientIdDetail`를 호출합니다.
    pub async fn binance_cancel_order_by_client_id_detail(
        &self,
        market: WireMarket,
        client_id: String,
    ) -> Result<WireBinanceOrderResponse, NativeError> {
        let market = market.into();
        let adapter = match self.built_in("binance_cancel_order_by_client_id_detail")? {
            BuiltInAdapter::Binance(adapter) => adapter,
            _ => return Err(provider_mismatch("Binance")),
        };
        adapter
            .cancel_order_by_client_id_detail(&market, &client_id)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Binance 전용 API인 `subscribeDetailed`를 호출합니다.
    pub async fn binance_subscribe_detailed(
        &self,
        subscription: WireSubscription,
    ) -> Result<NativeBinanceMarketSubscription, NativeError> {
        let subscription = subscription.into();
        let adapter = match self.built_in("binance_subscribe_detailed")? {
            BuiltInAdapter::Binance(adapter) => adapter,
            _ => return Err(provider_mismatch("Binance")),
        };
        adapter
            .subscribe_detailed(&subscription)
            .await
            .map(NativeBinanceMarketSubscription::new)
            .map_err(Into::into)
    }

    /// Binance 전용 API인 `subscribeDetailedWith`를 호출합니다.
    pub async fn binance_subscribe_detailed_with(
        &self,
        subscription: WireSubscription,
        config: WireStreamConfig,
    ) -> Result<NativeBinanceMarketSubscription, NativeError> {
        let subscription = subscription.into();
        let config = config.into();
        let adapter = match self.built_in("binance_subscribe_detailed_with")? {
            BuiltInAdapter::Binance(adapter) => adapter,
            _ => return Err(provider_mismatch("Binance")),
        };
        adapter
            .subscribe_detailed_with(&subscription, &config)
            .await
            .map(NativeBinanceMarketSubscription::new)
            .map_err(Into::into)
    }

    /// Binance 전용 API인 `subscribeDetailedAccount`를 호출합니다.
    pub async fn binance_subscribe_detailed_account(
        &self,
    ) -> Result<NativeBinanceAccountSubscription, NativeError> {
        let adapter = match self.built_in("binance_subscribe_detailed_account")? {
            BuiltInAdapter::Binance(adapter) => adapter,
            _ => return Err(provider_mismatch("Binance")),
        };
        adapter
            .subscribe_detailed_account()
            .await
            .map(NativeBinanceAccountSubscription::new)
            .map_err(Into::into)
    }

    /// Binance 전용 API인 `subscribeDetailedAccountWith`를 호출합니다.
    pub async fn binance_subscribe_detailed_account_with(
        &self,
        config: WireStreamConfig,
    ) -> Result<NativeBinanceAccountSubscription, NativeError> {
        let config = config.into();
        let adapter = match self.built_in("binance_subscribe_detailed_account_with")? {
            BuiltInAdapter::Binance(adapter) => adapter,
            _ => return Err(provider_mismatch("Binance")),
        };
        adapter
            .subscribe_detailed_account_with(&config)
            .await
            .map(NativeBinanceAccountSubscription::new)
            .map_err(Into::into)
    }

    /// Hyperliquid 전용 API인 `allMidsDetail`를 호출합니다.
    pub async fn hyperliquid_all_mids_detail(&self) -> Result<WireHyperliquidAllMids, NativeError> {
        let adapter = match self.built_in("hyperliquid_all_mids_detail")? {
            BuiltInAdapter::Hyperliquid(adapter) => adapter,
            _ => return Err(provider_mismatch("Hyperliquid")),
        };
        adapter
            .all_mids_detail()
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Hyperliquid 전용 API인 `perpetualMeta`를 호출합니다.
    pub async fn hyperliquid_perpetual_meta(
        &self,
    ) -> Result<WireHyperliquidProviderResponse, NativeError> {
        let adapter = match self.built_in("hyperliquid_perpetual_meta")? {
            BuiltInAdapter::Hyperliquid(adapter) => adapter,
            _ => return Err(provider_mismatch("Hyperliquid")),
        };
        adapter
            .perpetual_meta()
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Hyperliquid 전용 API인 `perpetualMetaAndAssetContexts`를 호출합니다.
    pub async fn hyperliquid_perpetual_meta_and_asset_contexts(
        &self,
    ) -> Result<WireHyperliquidProviderResponse, NativeError> {
        let adapter = match self.built_in("hyperliquid_perpetual_meta_and_asset_contexts")? {
            BuiltInAdapter::Hyperliquid(adapter) => adapter,
            _ => return Err(provider_mismatch("Hyperliquid")),
        };
        adapter
            .perpetual_meta_and_asset_contexts()
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Hyperliquid 전용 API인 `clearinghouseStateDetail`를 호출합니다.
    pub async fn hyperliquid_clearinghouse_state_detail(
        &self,
    ) -> Result<WireHyperliquidProviderResponse, NativeError> {
        let adapter = match self.built_in("hyperliquid_clearinghouse_state_detail")? {
            BuiltInAdapter::Hyperliquid(adapter) => adapter,
            _ => return Err(provider_mismatch("Hyperliquid")),
        };
        adapter
            .clearinghouse_state_detail()
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Hyperliquid 전용 API인 `frontendOpenOrdersDetail`를 호출합니다.
    pub async fn hyperliquid_frontend_open_orders_detail(
        &self,
    ) -> Result<WireHyperliquidProviderResponse, NativeError> {
        let adapter = match self.built_in("hyperliquid_frontend_open_orders_detail")? {
            BuiltInAdapter::Hyperliquid(adapter) => adapter,
            _ => return Err(provider_mismatch("Hyperliquid")),
        };
        adapter
            .frontend_open_orders_detail()
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Hyperliquid 전용 API인 `placeOrderDetail`를 호출합니다.
    pub async fn hyperliquid_place_order_detail(
        &self,
        request: WireOrderRequest,
    ) -> Result<WireHyperliquidOrderActionResponse, NativeError> {
        let request: maxt::OrderRequest = request.try_into()?;
        let adapter = match self.built_in("hyperliquid_place_order_detail")? {
            BuiltInAdapter::Hyperliquid(adapter) => adapter,
            _ => return Err(provider_mismatch("Hyperliquid")),
        };
        adapter
            .place_order_detail(&request)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }

    /// Hyperliquid 전용 API인 `cancelOrderDetail`를 호출합니다.
    pub async fn hyperliquid_cancel_order_detail(
        &self,
        market: WireMarket,
        order_id: String,
    ) -> Result<WireHyperliquidProviderResponse, NativeError> {
        let market = market.into();
        let adapter = match self.built_in("hyperliquid_cancel_order_detail")? {
            BuiltInAdapter::Hyperliquid(adapter) => adapter,
            _ => return Err(provider_mismatch("Hyperliquid")),
        };
        adapter
            .cancel_order_detail(&market, &order_id)
            .await
            .map(Into::into)
            .map_err(Into::into)
    }
}

/// 제공자 원본 스트림에서 다음 event/error/end 항목을 읽습니다.
pub async fn native_upbit_market_subscription_next(
    subscription: &NativeUpbitMarketSubscription,
) -> WireUpbitMarketStreamItem {
    subscription.next().await
}

/// 제공자 원본 스트림을 닫고 정리가 끝날 때까지 기다립니다.
pub async fn native_upbit_market_subscription_close(
    subscription: &NativeUpbitMarketSubscription,
) -> Result<(), NativeError> {
    subscription.close().await
}

/// 제공자 원본 스트림에서 다음 event/error/end 항목을 읽습니다.
pub async fn native_upbit_account_subscription_next(
    subscription: &NativeUpbitAccountSubscription,
) -> WireUpbitAccountStreamItem {
    subscription.next().await
}

/// 제공자 원본 스트림을 닫고 정리가 끝날 때까지 기다립니다.
pub async fn native_upbit_account_subscription_close(
    subscription: &NativeUpbitAccountSubscription,
) -> Result<(), NativeError> {
    subscription.close().await
}

/// 제공자 원본 스트림에서 다음 event/error/end 항목을 읽습니다.
pub async fn native_bithumb_market_subscription_next(
    subscription: &NativeBithumbMarketSubscription,
) -> WireBithumbMarketStreamItem {
    subscription.next().await
}

/// 제공자 원본 스트림을 닫고 정리가 끝날 때까지 기다립니다.
pub async fn native_bithumb_market_subscription_close(
    subscription: &NativeBithumbMarketSubscription,
) -> Result<(), NativeError> {
    subscription.close().await
}

/// 제공자 원본 스트림에서 다음 event/error/end 항목을 읽습니다.
pub async fn native_bithumb_account_subscription_next(
    subscription: &NativeBithumbAccountSubscription,
) -> WireBithumbAccountStreamItem {
    subscription.next().await
}

/// 제공자 원본 스트림을 닫고 정리가 끝날 때까지 기다립니다.
pub async fn native_bithumb_account_subscription_close(
    subscription: &NativeBithumbAccountSubscription,
) -> Result<(), NativeError> {
    subscription.close().await
}

/// 제공자 원본 스트림에서 다음 event/error/end 항목을 읽습니다.
pub async fn native_binance_market_subscription_next(
    subscription: &NativeBinanceMarketSubscription,
) -> WireBinanceMarketStreamItem {
    subscription.next().await
}

/// 제공자 원본 스트림을 닫고 정리가 끝날 때까지 기다립니다.
pub async fn native_binance_market_subscription_close(
    subscription: &NativeBinanceMarketSubscription,
) -> Result<(), NativeError> {
    subscription.close().await
}

/// 제공자 원본 스트림에서 다음 event/error/end 항목을 읽습니다.
pub async fn native_binance_account_subscription_next(
    subscription: &NativeBinanceAccountSubscription,
) -> WireBinanceAccountStreamItem {
    subscription.next().await
}

/// 제공자 원본 스트림을 닫고 정리가 끝날 때까지 기다립니다.
pub async fn native_binance_account_subscription_close(
    subscription: &NativeBinanceAccountSubscription,
) -> Result<(), NativeError> {
    subscription.close().await
}
